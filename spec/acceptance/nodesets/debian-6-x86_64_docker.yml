HOSTS:
  debian-6-x64:
    platform: debian-6-amd64
    hypervisor : docker
    image: debian:6
    docker_image_commands:
      - 'apt-get install -y wget locales rsync'
CONFIG:
  type: foss
  log_level: debug
