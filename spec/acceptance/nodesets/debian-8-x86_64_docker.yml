HOSTS:
  debian-8-x64:
    platform: debian-8-amd64
    hypervisor : docker
    image: debian:8
    docker_image_commands:
      - 'apt-get install -y wget locales rsync'
CONFIG:
  type: foss
  log_level: debug
