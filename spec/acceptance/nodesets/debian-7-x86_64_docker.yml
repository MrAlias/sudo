HOSTS:
  debian-7-x64:
    platform: debian-7-amd64
    hypervisor : docker
    image: debian:7
    docker_image_commands:
      - 'apt-get install -y wget locales rsync'
CONFIG:
  type: foss
  log_level: debug
